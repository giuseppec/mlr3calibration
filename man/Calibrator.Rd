% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Calibrator.R
\name{Calibrator}
\alias{Calibrator}
\title{Calibrator base class}
\description{
R6 base class for probability calibration.
Calibrators train on class labels and class-probability matrices and predict calibrated probabilities.
Training requires every declared class to be observed.
Prediction requires the same named class set as training; column order may differ.

\code{Calibrator} handles labels, probability matrices, and class names.
\link{PipeOpCalibrate} owns tasks, held-out predictions, learners, resampling, and prediction objects.
\subsection{Lifecycle}{
\itemize{
\item \verb{$train()} validates inputs and class support, requires packages, calls \verb{$reset()},
dispatches to the subclass hook, then records fitted class order and configuration hash.
\item \verb{$predict()} rejects untrained or stale objects (configuration \verb{$hash} mismatch),
requires packages, dispatches, and normalizes output.
\item \verb{$reset()} clears fitted and backend state without changing the configuration \verb{$hash}.
}

Input-validation or missing-dependency failures leave an existing valid fit untouched.
A backend fitting failure after \verb{$reset()} leaves the object untrained with backend state cleared.
}

\subsection{Properties}{

\code{properties} are unique values from \code{"twoclass"} and \code{"multiclass"}.
Declaring \code{"multiclass"} also permits two-class input.
Declaring only \code{"twoclass"} rejects more than two classes.
}

\subsection{Subclass hooks}{

Subclasses supply:
\itemize{
\item public \code{initialize()} (defaults required only for built-in / selector choices);
\item private \code{.train(truth, prob, class_names)} and \code{.predict(prob, class_names)};
\item private \code{.reset()} when they own fitted state;
\item \code{.additional_hash_input()} when configuration is not fully covered by base fields and parameters.
}

The base passes factor truth aligned with rows, finite normalized probabilities,
probability columns that exactly match unique class names in a defined order,
and at least one training sample per declared class.

Private \code{.predict()} may return a numeric vector (binary only; first-class probability)
or a numeric matrix over the fitted class set.
Values must be finite and in \verb{[0, 1]} with positive row sums.
The base reorders columns and normalizes rows; do not rely on normalization to hide invalid output.
}

\subsection{State, packages, and registration}{

Fitted state is private, cleared by \code{.reset()}, and excluded from configuration hashes.
Nested R6 fields must deep-clone independently.
Subclasses with environments, external pointers, or other reference state need a \code{deep_clone()} hook.

Optional packages go in \code{packages}; constructors must not load them.
Objects from \code{clb(key, ...)} must have \code{id == key}.
Registration lasts for the session; use \code{\link[=register_calibrator]{register_calibrator()}}.
}
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{id}}{\code{character(1)}.
Identifier for the calibrator.}

\item{\code{label}}{\code{character(1)}.
Human-readable calibrator label.}

\item{\code{param_set}}{\code{\link[paradox:ParamSet]{paradox::ParamSet}}.
Calibrator parameter set.}

\item{\code{properties}}{\code{character()}.
Supported task kinds.
Unique subset of \code{"twoclass"} and \code{"multiclass"}.
Declaring \code{"multiclass"} also permits binary input.}

\item{\code{packages}}{\code{character()}.
Required packages, enforced by the base class at train and predict time.}
}
\if{html}{\out{</div>}}
}
\section{Active bindings}{
\if{html}{\out{<div class="r6-active-bindings">}}
\describe{
\item{\code{hash}}{\code{character(1)}.
Hash of the calibrator configuration.
Does not include fitted state.}

\item{\code{is_trained}}{\code{logical(1)}.
Whether the calibrator currently holds a fitted configuration fingerprint.}

\item{\code{class_names}}{\code{character()} or \code{NULL}.
Fitted class order, or \code{NULL} before training and after \verb{$reset()}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-Calibrator-new}{\code{Calibrator$new()}}
\item \href{#method-Calibrator-train}{\code{Calibrator$train()}}
\item \href{#method-Calibrator-predict}{\code{Calibrator$predict()}}
\item \href{#method-Calibrator-reset}{\code{Calibrator$reset()}}
\item \href{#method-Calibrator-clone}{\code{Calibrator$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Calibrator-new"></a>}}
\if{latex}{\out{\hypertarget{method-Calibrator-new}{}}}
\subsection{Method \code{new()}}{
Creates a new \code{Calibrator} object.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Calibrator$new(
  id,
  param_set = ps(),
  properties = character(),
  packages = character(),
  label = id
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{id}}{\code{character(1)}
Identifier for the calibrator.}

\item{\code{param_set}}{\code{\link[paradox:ParamSet]{paradox::ParamSet}}
Calibrator parameter set.}

\item{\code{properties}}{\code{character()}
Unique subset of \code{"twoclass"} and \code{"multiclass"}.}

\item{\code{packages}}{\code{character()}
Required packages.}

\item{\code{label}}{\code{character(1)}
Human-readable label.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Calibrator-train"></a>}}
\if{latex}{\out{\hypertarget{method-Calibrator-train}{}}}
\subsection{Method \code{train()}}{
Train the calibrator.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Calibrator$train(truth, prob, class_names = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{truth}}{\code{factor()}
Ground-truth labels.}

\item{\code{prob}}{\code{matrix()}
Class-probability matrix.}

\item{\code{class_names}}{\code{character()}
Class order used in \code{prob}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(\code{Calibrator}) Invisibly returns \code{self}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Calibrator-predict"></a>}}
\if{latex}{\out{\hypertarget{method-Calibrator-predict}{}}}
\subsection{Method \code{predict()}}{
Predict calibrated probabilities.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Calibrator$predict(prob, class_names = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{prob}}{\code{matrix()}
Class-probability matrix.}

\item{\code{class_names}}{\code{character()}
Class order used in \code{prob}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(\code{matrix()}) Calibrated probabilities in the caller's requested column order.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Calibrator-reset"></a>}}
\if{latex}{\out{\hypertarget{method-Calibrator-reset}{}}}
\subsection{Method \code{reset()}}{
Clear fitted state without changing the configuration \verb{$hash}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Calibrator$reset()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
(\code{Calibrator}) Invisibly returns \code{self}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Calibrator-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Calibrator-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Calibrator$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
