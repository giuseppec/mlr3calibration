% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_plot.R
\name{calibration_plot}
\alias{calibration_plot}
\title{Calibration plot}
\usage{
calibration_plot(
  learners = NULL,
  task = NULL,
  bins = 10L,
  smooth = FALSE,
  ci = FALSE,
  rug = FALSE,
  predictions = NULL
)
}
\arguments{
\item{learners}{\code{list} of \code{\link[mlr3:Learner]{Learner}}
Trained learners to evaluate.
Use this together with \code{task}.}

\item{task}{\code{\link[mlr3:TaskClassif]{TaskClassif}}
Task used for prediction and evaluation when \code{learners} is supplied.}

\item{bins}{\code{integer(1)}
Number of bins used to aggregate predicted probabilities.}

\item{smooth}{\code{logical(1)}
Whether to draw LOESS-smoothed curves instead of bin-wise line segments.}

\item{ci}{\code{logical(1)}
Whether to draw confidence intervals for the smoothed curves.}

\item{rug}{\code{logical(1)}
Whether to add a rug for the raw predicted probabilities.}

\item{predictions}{named \code{list} of \code{\link[mlr3:PredictionClassif]{mlr3::PredictionClassif}}
Predictions to evaluate.
Use the list names as plot labels.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot reliability curves for one or more probabilistic classification learners
or predictions.
}
\details{
Supply either \code{learners} with \code{task}, or a named \code{predictions} list.
For each learner or prediction, the function groups predicted probabilities
into \code{bins}, and compares the mean predicted probability with the empirical
event frequency in each bin.
}
\examples{
if (mlr3misc::require_namespaces("mlbench", quietly = TRUE) &&
  mlr3misc::require_namespaces("mlr3learners", quietly = TRUE) &&
  mlr3misc::require_namespaces("rpart", quietly = TRUE)) {
  data("Sonar", package = "mlbench")
  task = mlr3::as_task_classif(Sonar, target = "Class", positive = "M")
  learner = mlr3::lrn("classif.rpart", predict_type = "prob")
  learner$train(task)
  prediction = learner$predict(task)

  calibration_plot(list(learner), task, smooth = TRUE)
  calibration_plot(predictions = list(rpart = prediction), smooth = TRUE)
}

}
\references{
Niculescu-Mizil A, Caruana R (2005).
\dQuote{Predicting Good Probabilities with Supervised Learning.}
In \emph{Proceedings of the 22nd International Conference on Machine Learning}, 625-632.
\doi{10.1145/1102351.1102430}.
}
\concept{plotting functions}
