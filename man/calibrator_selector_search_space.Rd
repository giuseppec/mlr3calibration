% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrator_selector_search_space.R
\name{calibrator_selector_search_space}
\alias{calibrator_selector_search_space}
\title{Calibrator selector search space}
\usage{
calibrator_selector_search_space(choices = NULL, prefix = "calibrate")
}
\arguments{
\item{choices}{\code{character()} or \code{NULL}
Registered calibrator keys to expose.
Defaults to the selector defaults.}

\item{prefix}{\code{character(1)} or \code{NULL}
Optional namespace prefix for the returned search space.
Use \code{"calibrate"} for a default \code{po("calibrate")} inside a \code{GraphLearner},
or \code{NULL} for the unprefixed selector space.}
}
\value{
A \code{\link[paradox:ParamSetCollection]{paradox::ParamSetCollection}}.
}
\description{
Build the dependency-aware search space for \code{clb("selector")}.
This helper is intended for outer tuning workflows around \code{po("calibrate")},
and returns the calibrator-specific part of the search space with the
required parameter dependencies already attached.
}
\examples{
learner_space = paradox::ps(
  "calibrate.classif.rpart.cp" = paradox::p_dbl(0.001, 0.1)
)
search_space = paradox::ParamSetCollection$new(c(
  list(learner_space),
  list(calibrator_selector_search_space(choices = c("platt", "beta")))
))

search_space$ids()

}
