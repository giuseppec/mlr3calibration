% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_then_calibrate.R
\name{tune_then_calibrate}
\alias{tune_then_calibrate}
\title{Tune-then-calibrate convenience helper}
\usage{
tune_then_calibrate(
  learner,
  calibrator = NULL,
  strategy = "oof",
  resampling = NULL
)
}
\arguments{
\item{learner}{\code{\link[mlr3tuning:AutoTuner]{AutoTuner}}
AutoTuner to train on the full training task before calibration.
The tuned predictive object must predict probabilities.}

\item{calibrator}{\link{Calibrator}
Calibrator prototype. Defaults to \code{clb("platt")}.}

\item{strategy}{\code{character(1)}
Calibration strategy. One of \code{"oof"} or \code{"per_fold"}.}

\item{resampling}{\code{\link[mlr3:Resampling]{Resampling}}
Resampling strategy used by the subsequent calibration step.
The object is cloned during construction.
When that step executes an uninstantiated resampling itself, it stratifies by the task target by default,
matching \link{PipeOpCalibrate}.
If the resampling is already instantiated, its stored train/test splits are preserved.}
}
\value{
A \code{\link[mlr3:Learner]{Learner}}, currently a \code{GraphLearner}.
}
\description{
Returns a \code{\link[mlr3:Learner]{Learner}} (a \code{GraphLearner}) that, in one \verb{$train()} call,
tunes an \code{\link[mlr3tuning:AutoTuner]{AutoTuner}} on the full training task and then
calibrates that fixed tuned learner with \code{po("calibrate")}.
}
\details{
Training steps:
\enumerate{
\item tune the supplied \code{AutoTuner} on the full training task,
\item extract the tuned predictive object,
\item build a calibrated learner with those fixed hyperparameters, and
\item train that calibrated learner on the same task.
}

Hyperparameters are not jointly tuned with calibration.
Prefer wrapping \code{po("calibrate", ...)} in an outer \code{AutoTuner}.
}
\examples{
if (mlr3misc::require_namespaces("mlbench", quietly = TRUE) &&
  mlr3misc::require_namespaces("mlr3learners", quietly = TRUE) &&
  mlr3misc::require_namespaces("mlr3tuning", quietly = TRUE) &&
  mlr3misc::require_namespaces("rpart", quietly = TRUE)) {
  set.seed(1)

  data("Sonar", package = "mlbench")
  task = mlr3::as_task_classif(Sonar, target = "Class", positive = "M")
  learner = mlr3tuning::auto_tuner(
    tuner = mlr3tuning::tnr("grid_search", resolution = 2),
    learner = mlr3::lrn(
      "classif.rpart",
      predict_type = "prob",
      cp = paradox::to_tune(0.001, 0.1)
    ),
    resampling = mlr3::rsmp("cv", folds = 2),
    measure = mlr3::msr("classif.bbrier"),
    term_evals = 4
  )

  learner_cal = tune_then_calibrate(
    learner = learner,
    calibrator = clb("platt"),
    strategy = "oof",
    resampling = mlr3::rsmp("cv", folds = 3)
  )

  learner_cal$train(task)
  learner_cal$predict(task)$score(mlr3::msr("classif.bbrier"))
}

}
